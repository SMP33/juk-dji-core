#Generated by VisualGDB project wizard.
#Note: VisualGDB will automatically update this file when you add new sources to the project.

cmake_minimum_required(VERSION 2.7)
project(juk-dji-core)
set(LIBRARIES_FROM_REFERENCES "")

find_package(roscpp REQUIRED)
include_directories(${roscpp_INCLUDE_DIRS})

find_package(DJIOSDK REQUIRED)
include_directories(${DJIOSDK_INCLUDE_DIRS})

find_package(LinuxChrono REQUIRED)
include_directories(${LinuxChrono_INCLUDE_DIRS})



add_executable(juk-dji-core common/dji_linux_environment.cpp  common/dji_linux_helpers.cpp juk-dji-core.cpp)
target_link_libraries(juk-dji-core ${roscpp_LIBRARIES} ${DJIOSDK_LIBRARIES}  ${LinuxChrono_LIBS} "${LIBRARIES_FROM_REFERENCES}")
 